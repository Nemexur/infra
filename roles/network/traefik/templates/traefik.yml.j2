---
log:
  level: INFO

api:
  insecure: true
  dashboard: true

entryPoints:
  web:
    address: ":{{ traefik_web_port }}"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
  file:
    filename: /configurations/dynamic.yml
